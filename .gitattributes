# Auto detect text files and perform LF normalization
* text=auto

# Shell entry points must remain executable by Git Bash after Windows checkout.
*.sh text eol=lf

# Prebuilt release outputs: keep the JSON module manifest stable across hosts
# and never apply text conversion to the compiled DLL.
*.modules text eol=lf
*.dll -text
