{
  "name": "ue57-mcp",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Development-only tests for the Streamable HTTP UnrealMCP UE plugin",
  "license": "MIT",
  "scripts": {
    "build:plugin": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/build-plugin.ps1",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run check && vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "2.0.0",
    "@types/node": "24.10.1",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
